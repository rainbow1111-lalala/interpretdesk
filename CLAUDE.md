# meeting-interpreter 项目工作指令

## 这是什么

英文会议实时同传辅助台，Web 应用。开会时开着它，浏览器抓会议标签页的声音，左栏出“英文原句＋
中文译文”双语字幕，右栏是吸收了会前背景文件的对话框，随时帮我拟英文回复（附中文对照）。

两种运行形态，由环境变量 `MI_SINGLE_USER` 切换：

- **本机单人模式**（`MI_SINGLE_USER=1`）：自己开会时用，数据落在 `data/` 原来的位置。
- **公网多人模式**（默认）：每个浏览器一个匿名会话，底稿、索引、设置、会议库各存各的，
  见 `config.Workspace`。**对外部署时绝对不能开单人开关**，开了就是所有人共用一套底稿。

## 定位边界

### 2026-09-05 确认的改进范围（2026-09-13 已全部实施，实测数据见 ROADMAP）

下面这些现在是既成事实，不是待办。改动它们之前先读 ROADMAP 对应那一节，里面记着为什么这么定。

- 服务各类外文会议，提示词不默认用户是律师。身份、目标、已确认事实、不可承诺事项由用户填写。
- 本轮不改收音或增加自动说话人识别；未识别的发言不得在提示词里断言为对方。
- 新建会议与继续本场分开。`meeting-data/<会议编号>/` 存该场原文、摘要、索引和
  `conversation.json`（会前信息、有效指示、拟稿历史）；`active-meeting.json` 仅记录当前编号。
  模型设置和已有 SQLite 会议库仍放会话根目录，不修改数据库结构，不迁移或覆盖旧材料。
- 首场可采用已有会前材料，后续新建会议为空白；恢复历史会议使用该场材料，不使用当前底稿。
- 会中有效指示由用户显式保存，不从聊天记录猜测。手动要求可中断自动生成，旧流不可覆盖新稿。
- 发言正文、中文对照先流式显示，依据与待确认项收在可展开区域。程序核验引用编号和原文摘录，
  未核实的出处不展示为已确认依据。材料中的命令不作为用户指示。
- 停止后等待语音尾句收口并落库，服务端确认结束后才能生成纪要；同一浏览器同时只允许一路会议录音。
- 会前界面展示实际收音、字幕返回、底稿准备情况；材料说明区分存储位置与模型处理位置。
- 本轮验证使用独立临时工作区和虚构会议，模型及语音服务使用本地替身；不对真实 data 运行测试。
  真实模型延迟与翻译质量另列待实测，不把离线通过写成真实会议验证通过。

- 只服务真实开会场景，不做讯飞同传已有的字幕投屏、分享字幕。
- 多人模式只做会话隔离，不做注册登录账号体系。少存一份密码就少一层泄露面，
  会话 id 存 cookie 足够。
- API key 一律各人自己填，服务端不代付，也不共用。
- 判断依据：这个功能在下一次真实会议里用不上，就不做。

## 技术选型

模型接入全部可配置，不硬编码服务商。配置存 `data/settings.json`（权限 600，已在
`.gitignore` 里），界面右上角“模型设置”里改，改完即时生效不重启。API key 不回显、不进日志、
不进仓库。

- 文本层：OpenAI 兼容的 `/v1/chat/completions`，负责底稿提炼、术语校正后的译文、右栏拟稿。
  当前用阿里云百炼的 workspace 端点，快档 `qwen3.6-flash`、强档 `qwen3.8-max`。
- 语音层：可选引擎，见 `server/settings.py` 的 `SPEECH_ENGINES`。当前用
  `qwen3.5-livetranslate-flash-realtime`（百炼实时 WebSocket，音频进、源语与译文文本一起出）。
  Gemini Live Translate 那条已实现，留作备用。转写分片那条（OpenAI 兼容
  `/v1/audio/transcriptions`）还没接线。
- 音频来源：Chrome `getDisplayMedia` 抓会议标签页音频（Zoom/Teams 网页版）。macOS 上 Chrome
  抓不到桌面端应用的声音，需要桌面端时才考虑虚拟声卡，属红线操作，先问。
- 后端 Python FastAPI，前端 Vite＋React＋TypeScript，样式手写 CSS 不引 Tailwind。

## 实测过的坑（不要回退）

1. **两类引擎的事件语义相反**。百炼的转写与译文事件给的是当前这一句的**全文**，每次要覆盖；
   Gemini 给的是**增量片段**，要累加。混用会让字幕重复叠好几遍。`TurnBuilder(cumulative=)`
   就是这个开关，按引擎设置，改动前先想清楚。
2. **底稿提炼必须走流式**。非流式时整段 JSON 憋到最后才回，长文件直接撞 httpx 读超时（120 秒），
   前端只看到“正在读”卡住。改流式后同一份文件 9 秒完成。
3. **思考模式全档关闭**。请求里带 `enable_thinking: false`。别家服务商不认这个字段会返回 400，
   代码里做了去掉重发一次并记住该端点不支持，只付一次代价。
4. Gemini 那条（备用）：`inputAudioTranscription` 必须放在 `setup` 根层，放进
  `generationConfig` 会被 1007 拒绝；单条连接寿命约 10 分钟，靠 `sessionResumption` 续接。
5. 音频必须是 16kHz 单声道 16 位小端 PCM，按 100 毫秒一帧发。
6. 百炼这条流译文比原话先到（实测译文 2.4 秒、原话 6.6 秒），界面不要假设原话先于译文。

## 底稿三层结构（改动前先读）

0. 路径一律从 `config.Workspace` 取，模块里不许再出现进程级路径常量，函数显式收
   `ws: config.Workspace`，不走隐式上下文。串号就是把别人的客户材料给错人，宁可多传一个
   参数，也不要一个读错了当场没人发现的隐式全局。改动后跑 `server/tests/isolation_check.py`。
0.1 **工作区有两层，`State` 上分别叫 `sws` 与 `mws`，取错哪一层都是事故。**
   - `sws`（会话根）：模型设置、SQLite 会议库、纪要目录、`active-meeting.json`。属于这个人。
   - `mws`（`sws.for_meeting(id)`）：这一场的原文、摘要、索引、`conversation.json`。属于这一场。
   判断方法：换一场会还该留着的东西归 `sws`，换一场会就该整个换掉的归 `mws`。纪要与导出按
   URL 里的会议编号取 `_meeting_ws(s, id)`，不要用当前这场的 `s.context`，否则给历史会议出
   纪要会把这一场的立场写进上一场的记录。没有活动会议时写材料的接口一律回 409，不许退回会话根
   （会覆盖老用户留在那里的材料）。改动后跑 `isolation_check` 与 `meeting_isolation_check`。
1. 原文在 `ws.docs`，摘要在 `ws.brief`，向量在 `ws.index`（单人模式即 `data/` 原布局）。上传分
   两种模式（界面上选，接口是 `POST /api/context` 的 `replace` 字段）：默认换新，旧原文
   与旧摘要挪进 `data/trash/<时间戳>/`；同一场会分批补材料时改选追加。底稿是一场会一份，
   上一场的材料留着会混进拟稿上下文。换新要等新文件确实抽出内容再清旧的，先清后抽遇上
   读不出的 PDF 就两头空。
1.1 底稿一变，跟着它走的东西必须一起换掉，统一走 `state.drop_briefing_traces()`：预取片段
   `state.excerpts` 是上一份原文的切块；术语表在字幕流水线里另存一份快照（`TurnBuilder`
   连接时取的），所以 `state.builder` 要登记给 state，清空或换新时把新表推过去，否则会中
   清掉底稿后字幕还在按已删底稿改译法。前端那边，换新与清空还要清掉拟稿卡片（它们作为
   对话历史发给模型），补材料时不清。改这一处后跑 `server/tests/clear_check.py`。
2. 拟稿上下文的组装顺序不能颠倒：原文全文、底稿摘要在最前（稳定前缀，服务端上下文缓存靠它命中），
   最近对话、此前交互、我的要求在最后。
3. 原文超过 `context_full_chars` 时才走向量检索补片段，两者并用而不是二选一。检索失败不阻断
   拟稿，降级成只用已放进上下文的部分。
4. **检索不许放回拟稿的关键路径**。两条后台预取：`main.py` 的 `refresh_excerpts` 在每段
   收口后跟着对方的话预取；`/api/context/prefetch` 在右栏打字停顿 300 毫秒时跟着我的输入
   预取。拟稿只取现成结果。实测同步检索会把首字从 2.39 秒推到 4.13 秒。
5. 上下文预算的实测曲线（首字延迟）：1.5k 字 1.6 秒，19.3k 字 1.8 到 3.3 秒，25k 字加
   8 块片段 1.8 到 2.5 秒，40k 字 5.05 秒。默认 25k，是为了让两万四千字的法规要点完整进
   上下文。改这个默认值前先重测。
6. 索引按 markdown 标题切块并带小节标题，带标题的小节另建标题索引（向量按标题算，命中
   返回整节）。改切块逻辑前想清楚：FAQ 型手册靠标题对标题匹配，定长滚动切会把问答切散。
7. 段落收口后右栏自动写建议回复（App.tsx 的 AUTO_INSTRUCTION），每次触发新增一张卡
   追加进对话流，历史卡保留可回看、不原地覆盖，原话超过 12 字时触发，可在栏头关掉。

## 文件抽取纪律

- 任何一份文件出问题都不许影响同批其他文件，更不许让服务崩掉。抽不出内容时抛
  `extract.ExtractError`，消息直接给用户看，不要抛裸异常。
- PDF 的三级降级顺序（pypdf、pdftotext、OCR）不要改成一步到位。OCR 慢且贵，只在前两级
  都拿不到文字时才走。
- 改抽取逻辑后必须跑 `server/tests/extract_check.py`，七类输入全过才算完。
- 抽取是同步阻塞的，调用处一律走 `asyncio.to_thread`，否则几十页 PDF 会卡住整个服务。

## 语种

- 源语言一律不设，靠模型自动识别，`qwen_live.session_update` 里 `input_audio_transcription`
  不带 language 字段。不要改成写死源语言，否则中英混说的会议会认错。
- 字幕译文语种与拟稿语言是两个独立设置（`target_lang` 与 `reply_lang`），不要合并。
- **拟稿语言必须放在提示词最末尾**。放系统提示里会被后面大段英文底稿和英文对话盖过去，
  实测切成日文后仍然吐英文，挪到最后一句才稳。见 `drafting.build_prompt` 结尾那块。
- 自动建议不看语种。曾经用 `!t.echo` 挡掉中文，导致对方讲中文时右栏不动，已去掉。

## 会议纪要（导出）

- 纪要正文交模型，逐句记录不交模型。`export_doc.verbatim_markdown` 直接从数据库生成，
  保证与左栏一字不差。这条不要改成让模型复述笔录。
- 会议时间由 `minutes.meeting_window` 按记录起止算，提示词里明确要求模型照抄不许推算。
  参会人认不出必须写待确认，后续事项只写记录里说过的承诺。这三条是防编造的闸门。
- docx 走 `export_doc`，格式是用户的成稿标准：全篇仿宋、正文小四、行距 26 磅固定、标题
  15/14/12pt 加粗、两端对齐、页脚页码。中文字体必须同时设 `w:eastAsia`，否则 Word 里
  中文会回退成默认字体。
- pdf 由 docx 经 LibreOffice 转，不要另写一套 PDF 排版，否则两种格式会不一致。

## 工程纪律

- 延迟是第一指标。任何改动如果让译文上屏超过 3 秒，回退。
- 术语与当事人名称译法由 glossary 强制锁定，用确定性替换，不靠模型自由发挥。中文并列短语的
  “和”“与”“及”视为等价，两个槽位做叉乘展开，见 `context_store.expand_doublets`。
- 本机单人模式下会议记录只落 `data/`，不上传。公网多人模式下用户的材料落在服务器的
  会话目录里，这是运营者要承担的责任，别再往外多送一步：底稿与笔录不进日志、不进任何
  第三方统计。
- 测试脚本不许对着线上 `data/` 跑。要验删除、清空一类破坏性路径，把
  `config.SESSIONS_DIR` 或直接把 `config.Workspace` 指向临时目录再测，跑完断言线上
  `data/docs` 没变（现有几个 check 脚本都是这么写的，照抄）。已经吃过一次亏：端到端
  测试发的 `DELETE /api/context` 把会前底稿清了，当时还是真删，找不回来。现在删除会挪进
  `ws.trash/<时间戳>/`，但这是兜底，不是可以随便删的理由。
- 验证改动时用 `MI_PORT` 另起一个实例，不要重启正在开会的那个。
- 字符串替换式的批量改动，改完必须回读文件核对。有一次 `import asyncio` 的替换静默没匹配上，
  只有调用改了，结果任何文件上传都报 NameError。脚本打印成功不等于改动生效。
- 改完必须跑 `server/tests/replay.py`（英文、中文、拟稿三项）和 `server/tests/ui_check.py`，
  贴输出再说完成。`replay` 现在要先建会议（脚本里的 `ensure_meeting` 已经处理），收到 `ended`
  才算收口完成。
- Playwright 那几个脚本的截图目录读 `MI_SHOTS` 环境变量，不写死路径。
- 界面回归起侧实例时，用一个把 `config.DATA_DIR` 指到临时目录的启动器，不要对着线上 `data/` 跑；
  要让浏览器用上模型配置就在那个临时目录里开单人模式，单人开关绝不进部署。
