[project]
name = "meeting-interpreter"
version = "0.1.0"
description = "英文会议实时同传辅助台"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "websockets>=13",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
    "pypdf>=5.0",
    "python-docx>=1.1",
]

[tool.uv]
package = false
