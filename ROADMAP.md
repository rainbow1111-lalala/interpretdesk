# ROADMAP · 会议同传台

立项日期：2026-08-17
当前阶段：已上线 https://interpretdesk.com ，HTTPS、限流、会话隔离均已线上验证

## 当前阶段

后端、前端、模型接入全部打通，会话隔离改造完成，多人版已上线 https://interpretdesk.com
（阿里云新加坡节点）。HTTPS、限流、上传限额、会话隔离均已在线上实测通过。把地址给别的
律师之前还差的是界面上的材料留存提示与多路并发实测，见「尚未完成」。

## 已完成（均已实测，附数据）

- 实时字幕链路：阿里云百炼 `qwen3.5-livetranslate-flash-realtime`，音频进、源语与译文文本
  一起出。连通测试 3.9 秒拿到 `session.created`；灌英文语音，译文首次上屏 2.4 到 2.7 秒、
  原话 6.6 秒；灌中文语音，同语种自动只显示一行不重复。
- 模型接入可配置：文本层走 OpenAI 兼容 `/v1/chat/completions`，语音层可选引擎，配置存
  `data/settings.json`（权限 600），界面右上角“模型设置”里改，三个测试按钮都是真发请求。
  当前快档 `qwen3.6-flash`、强档 `qwen3.8-max`、向量 `text-embedding-v4`。
- 底稿三层吸收：原文留 `data/docs/`（上传累加，可逐份删除、可整份清空）；强档提炼摘要存
  `data/context.json`；原文全文按预算进拟稿上下文；超预算部分走向量检索（`data/index.json`，
  纯 Python 余弦，45 块）。检索有效性已证伪测试通过，只存在于单一附件里的释放安排被准确引用。
- 术语锁定：确定性替换，“和”“与”“及”视为等价，并列短语两槽位叉乘展开。三份原文产出 73 条
  替换规则，字幕里“陈述和保证”被校正为“陈述与保证”。
- 拟稿：首字 1.6 秒（底稿一千多字）到 1.8 至 3.3 秒（一万九千字全文进上下文），输出英文加
  中文对照，与底稿立场冲突时追一行提示。思考模式全档关闭，不支持该字段的服务商自动降级重发。
- 界面：庭审笔录版式，左栏时间戳栏加双语条目，右栏拟稿卡片，录音条含声源选择与字号调节。
  Playwright 驱动真实构建产物验证过四个界面，音频帧确实从 AudioWorklet 送达 WebSocket。
- 会议记录落本机 SQLite，可导出 markdown。
- Gemini Live Translate 引擎保留为备用，已实测可用。

2026-08-17 下午批次（均已实测，附数据）：

- 自动建议回复：对方讲完一段外语、段落收口后 700 毫秒，右栏自动用快档写一版可直接念的
  英文回复（附中文对照），同一张「自动」卡原地更新不刷屏，栏头可开关。ui_check 断言真实
  通过：自动卡与手动卡并存，内容确实引用了底稿（责任不以「明知」为限、罚则数额）。
- 检索三处升级：按 markdown 标题切块并给每块带上小节标题；带标题的小节另建标题索引
  （向量按标题算，命中返回整节），FAQ 型手册对方提问对手册预设问题的匹配比对正文准；
  检索结果按正文去重。实测问「数据出境有没有做安全评估」，最高分 0.732 命中
  《数据出境安全评估办法》一节。两份真实底稿共 171 块，其中标题索引 24 条。
- 右栏输入防抖预取：打字停顿 300 毫秒按当前输入预取片段（/api/context/prefetch），
  检索跟着我的问题走且不占拟稿关键路径。后台预取 k 从 3 提到 8。
- 上下文预算 20k 提到 25k：法规要点对照（24,299 字）完整进上下文不再半截截断。实测
  25,000 字全文加 8 块片段下拟稿首字 1.82 / 2.13 / 2.50 秒（三次），在历史正常区间。
- 术语两处加固：底稿里没翻的英文术语（4 字符以上）也进替换表校正为给定译法；替换器给
  英文词加词边界（act 不再打进 action）。术语锁定从 62 条升到 80 条。
- 对照术语确定性抽取（aligned_terms）已实现并单元测试通过（En（中）、中（En）、表格行
  三种写法，前导废词剥离、频次择优）。注：现有两份底稿是段落级平行对照（英文行对中文行、
  英文正文带全角括号整段中文），术语级对照对不存在，故对这两份抽出 0 条属正确行为；
  该抽取器对将来「术语（对照）」或表格式底稿自动生效。
- 修复「开始记录」弹两次共享面板：setLink 异步生效，连点两下会重入 start，用 ref 同步
  挡住。共享面板本身是浏览器安全要求去不掉，已把选错的路堵上：默认停在「Chrome 标签页」
  页签、藏掉整屏（macOS 抓不到声音）和本工具自己的标签页、允许会中直接换共享标签页。

2026-08-17 晚批次（均已实测）：

- 共享失败报错精准化：拿不到音轨时读出实际共享的 surface（窗口/整屏/标签页），分别给
  针对性指引，不再让用户猜。当天两次现场失败（音频 token 0）即属选错共享对象。
- 麦克风设备可选：macOS 连续互通把 iPhone 设成系统默认输入时，点开始会弹 iPhone 连接页
  且本机什么都收不到（真实翻车）。现在声源选麦克风/混合时出设备下拉，默认优先本机内建
  麦克风、避开 iPhone，选择存 localStorage。纯麦克风模式关回声消除与降噪（公放收音时
  回声消除会把本机 Chrome 放出的会议声当回声消掉），保留自动增益；混合模式维持原样。
- 新增 tab_audio_check 端到端测试：无桩，真标签页用 WebAudio 循环播放 en.pcm 真实语音，
  Chrome 启动参数自动授予该标签页共享，字幕真实上屏（12 字）即通过。须有头跑（无头不支持
  getDisplayMedia）；Playwright 自带 Chromium 在清理阶段偶发崩溃，不影响结论。
- ui_check 断言放宽：快档偶发不写 ---ZH--- 分隔，改为断言卡片写出实际内容（英文或中文），
  不死等中文栏。

2026-08-18 首次实战后批次（均已实测，附数据）：

- 字幕重复修复（实战报告的 bug）：实抓百炼原始事件核实，所有实时事件带的都是**从这段话
  开头累积到当前的全文快照**，收尾事件一段只发一次、带整段全文（此前误当作「当前一句全文」，
  replay 的显示截断掩盖了真相）。一段话超过 TURN_MAX_CHARS＝400 被中途切卡后，后续快照
  又把整段全文灌进新卡片，旧译文整段重播。修法：TurnBuilder 记录本段已上屏偏移
  （_flushed），切卡后只取全文快照的未上屏后缀；段落真正结束（turn_complete →
  close(final=True)）时清零；快照比偏移短视为引擎已开新段，偏移自动归零兜底。确定性
  单元测试三场景通过，replay 英/中两项通过。
- 拟稿口语化与去材料依赖（实战反馈：书面腔、拿相近段落顶数、答不到点上）：系统提示改为
  「在会上当场接话」定位——第一句直接回应对方刚问的点，短句、允许缩写、Well/Actually/So
  口语衔接；授权以执业律师身份凭专业知识回答材料外问题，禁止拿主题相近的现成段落顶数；
  底稿标题从「回答时以此为准」改为「会前背景材料，供参考」；自动建议指令去掉「结合会议底稿」。
  改前改后三问对比（快档）：程序类问题从避答变为直接给出时长与步骤；实务类从论文腔变为
  「Look, it's not standard practice...」当场接话；材料内问题从整段背诵 my_position 变为
  先接住对方的点再讲理由。首字 2.5/2.0/1.8 秒 → 1.2/1.2/0.9 秒。
- 自动建议升强档：qwen3.8-max 热连接首字实测 1.3 至 1.5 秒（连接池生效，与快档同档位），
  达到既定 2 秒标准，App.tsx 自动建议从 fast 换 good。新提示词把强档全文时间从 8 至 9 秒
  压到 4 至 4.6 秒（输出变短所致）。
- 全套回归：replay 英/中/拟稿三项、ui_check、tab_audio_check（字幕真实上屏）全部通过。
- 自动建议保留历史：每次触发新建卡片追加进对话流，不再原地覆盖，历史草稿会中可回看
  （仍只存前端状态，停止或刷新后清空，未落库）。
- 拟稿紧跟现场议程：最近对话从固定 14 段改为字符预算制（一段常只有一两句，14 段只
  覆盖一两分钟，会议走到新议题时拟稿还停在底稿旧议题）；系统提示新增「紧跟现场议程」条，
  底稿外议题照常回答、不往底稿旧议题拉。replay 拟稿项与 ui_check 通过。预算实测定档
  6000 字（约二十多分钟现场内容）：强档首字 6000 字时 1.3 至 1.6 秒与基线持平，
  8000 字跳到 2.6 至 2.8 秒，两档之间有延迟台阶。

- 底稿改为一场会一份：上传分「换成新底稿」（默认）与「补进现有底稿」两种模式，接口是
  `POST /api/context` 的 `replace` 字段。换新时旧原文与旧摘要挪进 `data/trash/<时间戳>/`，
  界面先确认再动手。安全次序：等新文件确实抽出内容再清旧的，唯一文件读不出时旧底稿原样
  保住（隔离测试四情形通过，全程指向临时目录，线上 data/docs 未被触碰）。
- 底稿残留清干净（三处，统一走 `state.drop_briefing_traces()`）：一是预取片段
  `state.excerpts`（上一份原文的切块，会跟着下一次拟稿发出去）；二是术语表在字幕流水线里
  的快照，`TurnBuilder` 在连接建立时取一次，会中清空或换掉底稿后仍按已删底稿改译法，现在
  `state.builder` 登记给 state 由服务端主动推新表；三是前端拟稿卡片作为对话历史发给模型，
  换新与清空时一并清掉，补材料时保留。新增 `server/tests/clear_check.py`，验六项（磁盘原文、
  索引、摘要、预取片段、回收站、字幕术语表），指向临时目录，线上 data/ 不动。
- 界面文案精简：模型设置里的延迟实测数据、compatible-mode 地址说明、API key 存放位置说明
  与实时引擎的官方延迟口径全部删掉，这些属于开发期笔记，已在 CLAUDE.md 与本文件留档。

2026-08-18 会话隔离改造（feature/session-isolation 分支，已实测）：

- 起因：决定把这套工具对外开放给别的律师用。原架构是彻底的单人单机形态，`state` 进程级
  单例、五个存储路径全是固定常量、零会话标识，直接上公网等于甲律师上传的客户材料乙律师
  打开就能看见。
- 做法：引入 `config.Workspace`，一个会话独占一个存储根（docs、index、context.json、
  settings.json、meetings.db、trash、minutes 全部按会话分）。四个存储模块加 drafting、
  minutes、export_doc 共 28 个函数改为显式收 `ws`，不用隐式上下文，理由是串号这类缺陷
  一旦隐式就没人当场发现。`main.py` 的单例 State 改为按会话注册表，22 个端点走
  `Depends(current)`，会话 id 由中间件发放并写 cookie（httponly、samesite=lax、
  HTTPS 下带 secure），WebSocket 自己认 cookie（中间件不管 WS）。
- 两种形态：`MI_SINGLE_USER=1` 是本机单人模式，数据仍落 `data/` 原位，自己开会照常用，
  零迁移（实测新代码直接读到现有三份底稿与 185 块索引）；默认是多人模式按会话隔离。
- 安全：会话 id 会拼进文件路径，用 `^[A-Za-z0-9_-]{16,64}$` 严格校验，伪造的 `../` 一律
  换发新 id。会话超过 7 天不活跃由后台任务连目录一起清掉。
- 新增 `server/tests/isolation_check.py`，16 项断言全过：乙看不到甲的底稿、摘要、检索结果、
  设置与 API key、会议库；甲清空不影响乙；伪造会话 id 被拒且没跳出根目录；磁盘上确是两个
  独立目录。另用真实 HTTP 双 cookie 罐复验通过。
- 回归：replay 英/中/拟稿、ui_check 在新代码单人模式下全过；clear_check、replace_check
  已适配新结构并通过。全程用 8788/8789 另起实例验证，没有重启正在开会的 8787。

## 部署上线（2026-08-19，已实测）

- 服务器：阿里云新加坡节点，Ubuntu，公网 IP 8.219.58.19（whois 归属 Alibaba Cloud
  (Singapore) Private Limited）。选境外节点故不涉及 ICP 备案。原判断「Cloudflare 放不了
  Python 长连接应用」成立，Cloudflare 现只做 DNS 解析，应用跑在自有主机上。
- 部署方式：`deploy/pack.sh` 打包（不含 data/）→ 解到 /opt/interpretdesk →
  `deploy/install.sh` 一次装完。服务以 systemd 管理，`interpretdesk.service`
  active 且 enabled，uvicorn 监听 127.0.0.1:8787，nginx 反代 80 端口。
- 实测证据：服务器本机 `curl 127.0.0.1:8787/api/health` 返回 200；带
  `Host: interpretdesk.com` 走 nginx 同样 200。本机远端复验
  `curl -H 'Host: interpretdesk.com' http://8.219.58.19/` 返回 200，响应头
  `Server: nginx/1.24.0 (Ubuntu)`，并带 `set-cookie: mi_sid=...; HttpOnly; SameSite=lax`，
  即多人会话中间件确已生效。安装脚本自测那次 404 事后不复现，带与不带 Host 现均为 200。
- 域名解析：interpretdesk.com 与 www 两条 A 记录均指向 8.219.58.19，Cloudflare 托管
  （NS 为 christina/donovan.ns.cloudflare.com），当前灰云 DNS only，回源 IP 直接暴露。

## 2026-08-19 声源默认改为麦克风（已实测）

- 起因：共享面板是浏览器的安全要求，代码里去不掉，8 月 18 日两次现场失败都出在选错共享
  对象。日常改走麦克风外放收音，开始记录就直接开始，不再经过面板。
- 做法：下拉框顺序改为麦克风（外放收音）、会议标签页（戴耳机时用）、混合声源，默认停在
  麦克风。抓标签页那条路整条保留，戴耳机开线上会时对方声音进不了麦克风，那种场合仍需要它。
- 连带处理：默认改麦克风后，进页面就枚举设备会立刻弹权限框，公网上每个访客一进来被索权限
  不能接受。`listMics` 加 allowPrompt 参数，进页面只枚举不索权限；拿不到设备名时不出设备
  下拉，等用户点开始记录（浏览器认的用户手势）再索一次权限，并按名字避开连续互通的 iPhone。
- 实测：ui_check 通过（笔录 4 条、音频帧 15 帧真实送出、自动建议与手动拟稿卡片并存），
  待机截图确认默认声源为麦克风、设备自动选中内建麦克风而非同时存在的 iPhone 麦克风。
  测试桩相应补上 getUserMedia 与 enumerateDevices，getDisplayMedia 保留。
  tsc 无错，前端构建通过。

## 2026-08-19 拟稿注意力与会中改口（已实测）

- 实战问题：会上口头推翻了会前拟的某个方案，右栏后面几张卡还围着旧方案答，且答非所问。
- 成因（有据）：一是底稿原文整段常驻上下文（默认 25,000 字），旧方案以上万字体量摆在
  模型面前，而现场那句改口只有一行；二是后端只取最近 6 条对话历史，自动建议每出一张卡
  就占掉两条，手打的指示三张卡之后就被挤出窗口，模型再也看不到它。
- 改法：手打的要求单列一节「会中指示，按先后顺序，一直有效，效力高于会前底稿」，不受历史
  窗口挤压；历史窗口 6 放宽到 12；系统提示加一条改口规则（与底稿冲突时以会中指示为准，
  后面的指示覆盖前面的，不再把旧方案当我方主张来讲，但对方直接问到时照实回应）。
- 注意力：`context_full_chars` 先降到 8,000，当天实测后改定 50,000，见下条。
- 实测：新增 `server/tests/directive_check.py`，用虚构情节，两层验。拼提示词层五项全过，
  并复现成因（不传指示时指示确会被历史挤掉）；模型层在指示后灌八张自动卡（十八条历史）
  仍按新方向作答。ui_check 回归通过。
- 新增 `server/tests/model_bench.py`：同一组现场提问并排打到多个候选端点，比首字延迟、
  全文延迟与实际答案。候选与提问都从仓库外的文件读，key 不进仓库也不打印。
  本机实测（底稿常驻 8,000 字，三条问题）：快档 qwen3.6-flash 首字均值 1.14 秒、全文
  2.47 秒；强档 qwen3.8-max 首字均值 1.15 秒、全文 4.16 秒。作为对照，8 月 17 日在
  25,000 字常驻下强档首字是 1.82 / 2.13 / 2.50 秒。两次提示词与问题不同，不是控制变量
  实验，只能作趋势参考。

## 2026-08-19 右栏贴底跟随（已实测）

- 实战问题：拟稿刷新快，想往上翻看之前的建议时，新卡片一到就把视线拽回最新那条。
- 改法：右栏记一个「是否贴底」状态，停在底部（余量 40 像素，流式写字时高度一直在长，
  严格等于底部几乎不成立）时新卡片照常跟随；往上翻就停住不再跟随；翻回底部自动恢复。
- 实测：新增 `server/tests/scroll_stick_check.py`，桩掉 /api/draft 用固定回答，驱动真实
  界面四项断言全过：右栏确实可滚动、停在底部时仍自动跟到底、往上翻后 scrollTop 0 → 0
  没被拽走、翻回底部后恢复跟随。

## 2026-08-19 底稿注入预算实测定档 50,000 字

- 起因：两万五千这个数字一直被当成上限，实际它只是本项目自设的注入预算。查证
  qwen3.8-max 的上下文窗口是 100 万 token（阿里云百炼官方文档），远不是瓶颈。
- 实测（同一问题、同一模型 qwen3.8-max、只改注入字数，两轮共六次每档）：
  8,000 字首字均值约 1.97 秒（波动 1.05 到 3.19，方差最大）；50,000 字约 2.07 秒
  （1.79 到 2.31，很稳）；全量 87,138 字约 2.66 秒（2.24 到 3.27）。全文延迟依次约
  4.4、4.5、5.6 秒。
- 结论：八千到五万之间没有延迟代价，差值落在噪声里；一次性全量灌进去才有代价，首字多
  约 0.7 秒、全文多约 1.2 秒。因此定档 50,000，覆盖现有底稿八万七千字的约六成，其余仍
  交检索现取。没有一路开到全量，理由是延迟之外还有注意力：底稿体量压过现场那一句正是
  当天失焦的成因之一，会中指示那一节压住的是改口场景，泛泛的跑题它压不住。
- 端点侧另有发现：现用的百炼 workspace 端点 `/v1/models` 返回 240 个模型，除 qwen 系列
  外还有 GLM-5.3、deepseek-v4-pro 等，换模型对比不必另开账号。

## 2026-08-19 系统提示词体检与四处修改（已实测）

- 重排：原编号 1、2、3、3.1、4、4.1、5、5.1、6 至 9 是历次打补丁堆出来的，3、3.1、5.1
  内容有重叠。改为三节十四条（输出形式、怎么说、立场与边界），语义保持不变。
- 【真冲突，已修】`build_prompt` 末尾无条件追加「正文必须用目标语言写完再给中文对照」，
  把系统提示里「问术语含义或让你判断形势时用中文简答、不要 ---ZH---」那条压过去了。
  改前实测：问「carve-out 是什么意思」回的是英文解释加 ---ZH--- 加中文对照；问「他这句
  是让步还是施压」中文答完又多拟一段英文。修法是引入 mode：界面「这句什么意思」传 ask，
  「回这一段」「追问」与自动建议传 draft，自由输入不传、末尾把两种情形都写明由模型自认。
  改后实测两条 ask 均为中文简答且不含 ---ZH---，draft 仍照常给英文加对照。
- 【口头禅，已修】原规则把 Well 列在口语衔接词第一位，实测 model_bench 四问四次以 Well
  开头。改为「开头不要每次都用同一个词，多数时候直接进入正文」。改后五个不同问题的开头
  词为 the、three、we、the、liability，四个不同，Well 一次未出现。
- 【防编造，新增】原提示词没有任何事实约束，却授权凭专业知识回答材料外问题。新增第 11 条：
  具体数字、日期、期限、金额、法条编号，以及任何一方说过或承诺过什么，底稿或现场对话里
  没有出处就不写，改用「需要确认后回复」带过，此条高于「答得完整」。实测问「上季度处理了
  多少条个人信息、什么时候完成备案」，回的是暂无确切数字、需要确认后回复，没有编数。
  另核实：回答里出现的 Article 51、Article 27 在底稿中确有出处（阿拉伯数字写法），非杜撰。
- 【冲突提示扩面】原第 6 条只覆盖与底稿立场冲突，现同时覆盖与【会中指示】中更早一条的冲突。
- 新增 `server/tests/prompt_mode_check.py`，两层八项全过。directive_check、ui_check、
  scroll_stick_check 三项回归全过。

## 2026-08-19 正式上线并回归（已实测）

- HTTPS 已签发并生效：Let's Encrypt 证书 CN=interpretdesk.com，有效期 2026-08-19 至
  2026-11-17，自动续期定时器 certbot.timer 已就位。HTTP 301 跳 HTTPS。
- 六笔改动随本次上线：声源默认麦克风、底稿份数上限、会中指示修复、右栏贴底跟随、
  底稿注入预算 50,000 字、系统提示词四改。另有一直未生效的 nginx 限流本次一并装上。
- 上线时踩到两个坑，都已修并回写代码：
  一是 `http2 on;` 指令要 nginx 1.25.1 以上，服务器是 1.24.0，配置直接语法不过（脚本
  自动回滚，站点没受影响）。
  二是改用 `listen 443 ssl http2;` 之后 WebSocket 经 nginx 握手从 101 变 404。根因是
  HTTP/2 协议不允许 Upgrade 升级机制，nginx 只能当普通 GET 转发；强制 HTTP/1.1 则正常。
  浏览器开 WebSocket 一律走 HTTP/1.1 未必踩到，但这条长连接是本程序命根子，且 h2 对
  单包页面收益很小，最终决定关掉 http2。
- 上线回归（全部从本机对线上域名实测）：HTTPS 首页 200、证书链校验通过；HTTP 301 跳转；
  WebSocket 带 cookie 握手 101、不带 cookie 403（会话闸门生效）；两个独立 cookie 罐拿到
  不同会话 id；上传 11 份底稿回 413 并给出「最多存 10 份」提示；并发 60 次请求出现
  46 次 200、14 次 429（限流生效）；线上前端包 index-CnFMYOH8.js 与本机构建产物一致。
- 未配模型的新访客上传底稿回 502 并提示「还没配文本模型」，属预期路径。

## 2026-08-19 手机端与首次使用提示（已实测）

- 手机端此前完全没做：整份样式里一条 @media 都没有，右栏最小宽度写死 360 像素，
  390 宽的手机必然挤爆。
- 布局：820 像素以下改上下分屏，笔录在上、拟稿在下各占一半（横屏手机改 0.8 比 1.2）。
  高度用 100dvh，避开 iOS 地址栏伸缩。录音条改为贴底整条、折行两排（上排开始键与计时，
  下排两个下拉），底部留 safe-area。工作区留出等高空档，否则拟稿输入框被录音条盖住。
- 能力探测：手机浏览器没有 getDisplayMedia，新增 `canCaptureTab()`，界面上只保留麦克风，
  不再给出点了必然报错的「会议标签页」。
- 首次进入弹一次使用提示（`web/src/Onboarding.tsx`，localStorage 记住），只举例不解释界面：
  底稿传什么不传什么、会中怎么用。留存提示那句当天按要求删除，见「尚未完成」。
- 实测 `server/tests/mobile_check.py`，iPhone 14 与 iPhone SE 两个尺寸各十项全过：首次弹提示、
  点知道了后刷新不再弹、无横向溢出、两栏上下都在、录音条与开始键与声源下拉完整可见、
  拟稿输入框与发出键没被盖住、声源只剩麦克风。截图留档。
  教训记一笔：第一版断言只查了容器宽度，截图里「开始记录」被切在左边缘、输入框被录音条
  整个盖住，断言却全绿。断言要落到具体控件的边界，不能只查容器。
- ui_check 与 scroll_stick_check 因弹窗遮罩需先关闭提示，已相应调整，两项回归通过。
- 已上线并在线上复验（真手机视口驱动 https://interpretdesk.com ）：首次进入弹出使用提示、
  横向溢出 0、上下两栏各 326 像素、开始记录键完整可见、声源只剩麦克风两项。线上样式文件
  与本机构建产物逐字节一致，含 max-width:820px、100dvh、safe-area-inset-bottom。
  HTTPS 200、HTTP 301 跳转、WebSocket 带 cookie 握手 101 均无回归。

## 2026-08-19 模型设置简化（已实测）

- 实战反馈：有用户把文本层配成智谱，点开始记录没有字幕。看截图查明三处，都不是模型问题：
  一是语音层的 base url 与 api key 只是灰色占位没真填，而实时字幕是完全独立的一条链路，
  文本层配好它也不工作；二是向量模型填的是阿里的 text-embedding-v4，配在智谱端点上检索
  必然失败；三是界面上没有任何地方说缺了什么。
- 改动：顶部加配置状态条（读现成的 /api/health），缺什么直接写后果；文本层加服务商预设
  下拉（阿里云百炼、智谱、DeepSeek、OpenAI、自定义），选了自动填 base url；打开设置时
  把语音层的默认 base url 与 model 真的填进输入框，不再只当灰字摆着；文本层选了做不了
  实时字幕的服务商时，语音层顶上直接红字说明要另配百炼或 Gemini（措辞落在本程序自己身上，
  写「实时字幕目前只接了这两家」而不是「某家没有这个能力」，因为 OpenAI 有流式转写、
  智谱有 GLM-Realtime，只是都还没接进来，替人家下能力判断是不准确的）；三个模型输入框接上
  datalist，「拉模型清单」拉到之后点一下就能从真实清单里选，不必手打。
- 预设只写 base url 不写模型名：模型名会过时，端点返回的清单不会。
- 智谱语音能力查证（官方文档）：GLM-ASR-2512 是整段文件转写，单次上限 25 MB、时长不超过
  30 秒，只收 wav 与 mp3，不是实时流；GLM-Realtime 是实时音视频对话，自有协议且输出的是
  对话回复不是译文。两者都不适合本程序的实时字幕，故不接。
- 向量模型不再要用户填：新增 `llm.resolve_embed_model`，留空时从端点模型清单里挑名字带
  embed 的那个，按端点缓存；挑不到就返回空，检索这一路自然停用，底稿与拟稿不受影响。
  默认值由 text-embedding-v4 改为空。该字段与上下文字数上限一起收进「高级」折叠区。
  实测：本机端点留空时自动选到 qwen3.7-text-embedding，用它检索命中 3 块，链路通。
- 上线时线上验出一处本次改动引入的问题并已修：把默认值真填进输入框之后，百炼那条默认
  base url 是 `https://<workspace>.cn-beijing.maas.aliyuncs.com/...`，`<workspace>` 是模板
  不是真域名，填进框会被用户当成已配好存下去。改为带 `<` 的默认值一律只作灰字提示不入框，
  测试加断言看住。
- 线上复验（全新会话）：状态条为红字「还没配：文本模型、语音模型。缺文本模型则拟稿不出，
  缺语音则点开始记录不会有字幕」；框内预填值只剩可用的真值（模型名、译文语言、字数上限）；
  向量模型收在高级里；HTTPS 200、HTTP 301、WebSocket 101 均无回归。
- 实测 `server/tests/settings_ux_check.py` 六项全过：语音层默认值确实填进框里、选智谱后
  出现另配语音的红字提示、服务商预设自动填好 base url、向量模型默认收在高级里、点开可见。
  截图留档。

## 尚未完成（对外开放前必须补）
- ~~上传体积限制~~ 已完成：单次 20 MB、单会话累计 60 MB，超限回 413 且不动已存底稿
  （isolation_check 已加两条断言）。
  注：每个用户填自己的 API key，模型费用不落在运营者身上，限流主要防的是服务器磁盘与 CPU。
- ~~单会话底稿份数上限~~ 已完成并实测：`MAX_SESSION_DOCS = 10`，超出回 413 且一份都不落盘
  （replace=True 换新底稿时只数本次这批）。isolation_check 新增两条断言，全 20 项通过。
  理由是体积限额挡不住几百份小文件把向量索引撑爆。
- 限流：nginx 配置已写进 `deploy/nginx.conf`（按来源 IP 常规 20r/s 突发 40、上传路径
  6r/min 突发 3、并发连接 20，超限回 429），**尚未在服务器上生效**，要等签完证书后随最终
  配置一起装上并实测，装上前不得标为已完成。
- ~~隐私政策与用户协议~~ 2026-08-19 灵宝拍板不做，本项关闭，不再列为上线前置。
- 前端材料留存提示：曾写进首次使用提示，2026-08-19 按灵宝要求删除该句，现界面上不再
  告知材料上传至服务器、保留多久、如何删除。该项回到未完成状态，由灵宝决定是否另行安排。
- 并发实测：目前只验了会话隔离的正确性，没有压过多路 WebSocket 同时转写的资源占用。

## 进行中

无。

## 本轮新增（2026-08-17 晚）

**暂停键**。暂停时前端不再上送音频、计时停住、电平归零，并通知后端把当前这段收口，恢复后
从新的一段开始，不会把暂停前后的话粘成一段。WebSocket 保持连接，恢复是即时的。实测状态
流转：正在同传 → 已暂停 → 正在同传。

**会议纪要**。点停止记录即自动开始整理，不需要指令，用户只挑下载格式。实测 6.3 秒出稿。
文档两部分：纪要正文由强档模型按议题归类整理；文末附逐句原话与译文，该部分由程序直接从
数据库生成不经模型，与左栏一字不差。三条防编造要求写进提示词并实测生效：会议时间由程序
按记录起止算，参会人认不出写待确认（实测确实没编人名），后续事项只写记录里说过的承诺。
可下载 md、docx、pdf。docx 实测核验：仿宋（中英文字体都设了 eastAsia）、标题 15pt 居中
加粗、正文小四、行距 26 磅、页脚页码；pdf 由 docx 经 LibreOffice 转，两页真文本。文件名
形如【260817】关于某某事项的会议纪要.docx。

**拟稿答非所问的修复**。真实会议里发现自动建议回答的不是对方刚问的问题（对方问这名专员会
遇到哪些实际障碍，回复却在讲这个岗位的定位）。三个成因分别修掉：

1. 提示词把最近 14 段平铺成一堆「对方说」，模型不知道该回应哪一句。现在最后一句单独拎成
   「对方刚说完这一句，回应的就是它」，其余标为只作背景。
2. 底稿是问答手册，模型倾向拿主题相近的现成答案顶上。系统提示新增一条：先看懂对方问的是
   什么并回答那一个问题，底稿是参考材料不是答案库，没有直接对应就依据底稿事实自己推理。
3. 自动建议在上一版还在写时会把新触发直接丢掉，不排队不重试，导致卡片停在旧问题上。改成
   等写完再补一次，新的收口会取代待补的那次。
4. 自动卡片现在显示它在回应哪一句，卡片停在旧问题上一眼能看出来。

用截图里那段真实对话复现验证：修复前答的是岗位定位，修复后逐条答出个保法单独同意与影响
评估、敏感与重要数据不得径直出境、域外管辖下的个人责任三项障碍。首字 1.24 秒。

**拟稿长度放开**。原来限定两到四句，现在按问题本身定长度，清单式问题逐条展开并说明理由。
实测同一问题从 60 词左右增加到 129 词，三条各自展开。

## 多语种与拟稿口径（2026-08-17 夜）

- 字幕译文语种可选十四种，源语言自动识别，日译中、韩译中、法译中、中译英都只是换目标语。
  拟稿语言独立设置，可选七种，中文对照始终附后。顶栏两个下拉直接切，字幕语种录音中不可改。
- 拟稿语言的指令必须放在提示词最末尾。实测放系统提示里会被后面的英文底稿盖过去，切日文
  仍吐英文；挪到末尾后日文输出正常。
- 自动建议去掉了「只在对方讲外语时触发」的闸门，讲中文也照常拟稿。
- 拟稿长度按问题定，清单式问题逐条展开。

## 待办

1. 真实会议验证网页版 Zoom 或 Teams 的标签页音频链路（需本人操作共享面板）。
   进展（2026-08-17 晚）：代码侧已用 tab_audio_check 自动化验真，真标签页放真实语音、
   auto-select 授予共享、字幕真实上屏，链路本身可用。当天用户两次现场尝试音频 token 均为 0，
   判断为共享面板选择对象不对（可能停在 Window 页签，macOS 窗口共享带不了声音）；报错信息
   已改为读出实际共享的 surface 并给出针对性指引。剩余事项只是真实会议里点对一次。
2. ~~上下文预算的真实曲线~~ 已完成：测了四个点，默认值据此从四万字下调到两万字。
3. 转写分片引擎（OpenAI 兼容 `/v1/audio/transcriptions`）未接线，留给本地 whisper 或其他
   服务商的后路。
4. 会后产物：目前只有 markdown 导出，是否需要按会议纪要体例出 docx 待定。

## 阻塞

- 覆盖 Zoom、Teams 桌面端应用的声音需要装 BlackHole 一类虚拟声卡，属红线操作，等本人拍板。

## 已修复的缺陷

- 清空底稿是不可恢复的真删，且界面按钮没有二次确认。2026-08-17 因此丢过一次会前底稿：抽取
  加固的端到端测试对着线上 data 目录发了 `DELETE /api/context`，把已上传的原文连同索引一并
  清掉，事后无从取回，只能重新整理重新上传。现在原文与摘要一律挪进 `data/trash/<时间戳>/`，
  界面清空按钮加了二次确认。
- 上传后界面无成功提示，读完文件被清空、按钮变灰，看起来像没反应。现在读完显示确认行，
  并把逐份读取失败单独报出来。
- 底稿提炼非流式导致长文件撞 120 秒读超时。改流式。
- 大文件解析同步阻塞事件循环。改 `asyncio.to_thread`。
- 向量索引批次串行，74k 字要 48.6 秒。改并行，降到 22 秒。
- 向量检索占在拟稿关键路径上，首字 4.13 秒。改后台预取，降到 2.39 秒。

## 文件抽取（2026-08-17 加固）

七类输入实测全过，无一崩溃：普通文本、GBK 编码文本、docx、文字版 PDF、扫描件 PDF（OCR 认出
内容）、损坏 PDF（明确拒绝）、加密 PDF（明确拒绝）、百万字文本。端到端经上传接口验证：两份
PDF 入库、两份被拒且说明原因、其他文件不受影响。向量索引设 800 块上限，避免超大文件触发
几千次向量调用。

## 最近验证

2026-08-17：字幕（英文、中文两例）、拟稿、底稿提炼与累加、向量检索、界面渲染，全部通过，
数据见上。测试命令见 README 的自测一节。

2026-08-17 回收站改动后复测：`extract_check` 七项全过；`replay` 英文样例首次译文 3.51 秒、
原话 6.91 秒，中文样例同语种只出一行，均通过；`replay --draft` 在装载 77,000 字真实底稿的
情况下首字 2.09 秒、全文 3.02 秒，通过；`ui_check` 四个界面渲染并确认音频帧真实送出，通过。
回收站逻辑另做隔离测试（临时目录，不碰线上 data）：单份删除、整份清空、内容取回、空目录不
建空文件夹，四项全过。真实底稿两份（问答手册 52,822 字、法律法规要点 24,299 字）经上传接口
入库，28 秒完成，术语锁定 62 条、原文索引 147 块，服务重启后仍在。

注意：英文首次译文一度测到 3.51 秒，下午批次复测回落到 2.59 秒（中文 2.93 秒），确认是
模型端波动不是回归。

2026-08-17 晚提速：LLM 连接复用（进程内共用 httpx 客户端、保活五分钟）后，拟稿首字从
1.82 到 2.50 秒降到热连接 0.92 到 1.57 秒，全文 2 到 2.6 秒；自动建议去抖 700 毫秒压到
250 毫秒。字幕 2.50 秒无回归。字幕首字约 2.5 秒是 livetranslate 引擎的下限（它要听够音频
才敢出译文），客户端侧没有可动的杠杆。

2026-08-17 下午批次复测：extract_check 七项全过；replay 英文首次译文 2.59 秒、中文 2.93 秒；
replay --draft 三次首字 1.82 / 2.13 / 2.50 秒（25,000 字全文加 8 块片段）；ui_check 新增
自动建议断言后全过（自动卡出现并写出内容、手动卡并存、音频帧真实送出）。检索、对照抽取、
词边界、小节切分各有隔离单元测试，五项全过。
